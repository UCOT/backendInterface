export default class Traceability {
  /**
   * Description: get trace data for map
   * @access     public
   * @param {string} company_id id of the company_id
   * @returns {Object}:
   *      Returns trace data grouped by batches
   */
  static async traceability_map(company_id) 

  /**
   * Description: get alarms of trace data
   * @access     public
   * @param {string} company_id id of the company_id
   * @returns {Object}:
   *      Returns alarms
   */
  static traceability_alarm(company_id)
}